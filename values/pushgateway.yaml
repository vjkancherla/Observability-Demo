# Minimal Pushgateway values for observability integration
serviceMonitor:
  enabled: true
  namespace: observability
  honorLabels: true
  additionalLabels:
    release: dev-prometheus  # This makes Prometheus pick it up

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi